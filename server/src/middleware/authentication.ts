//import { Socket } from "socket.io";
//import { DefaultEventsMap } from "socket.io";
//import next
export default function authentication(socket:any,next:any) {
    console.log("authenticating...")
    next();
}